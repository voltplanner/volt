export const RenderModal = ({ currentModalKey} : {currentModalKey: string } ) => {
    // const isModalOpen = useModalState(state => state.isModalOpen(currentModalKey));
  
    // const modals = {
    //   [ModalKeys.Invite]: (isModalOpen && <ModalInvite />),
    // };
  
    // return modals[currentModalKey];
    return null
  };
  